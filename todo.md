  project-manager init(create project.ini, example code)
  unit testing
  default config(global project.ini)
+ in order args execution(f.e. `project-manager build run script clean` -> buld -> run -> script clean)

